# Lightweight requirements for Railway/Render free tier
# Excludes heavy ML dependencies (torch, sentence-transformers)
# Uses keyword search instead of vector embeddings

# Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0

# Database
sqlalchemy>=2.0.23

# Configuration
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Scraping
feedparser>=6.0.10
beautifulsoup4>=4.12.2
html5lib>=1.1
requests>=2.31.0
python-dateutil>=2.8.2

# Social Media APIs (optional - will gracefully fail if not configured)
praw>=7.7.1
tweepy>=4.14.0

# AI (Groq is free and lightweight - no local models)
openai>=1.3.5

# Vector DB (persistent semantic retrieval)
chromadb>=0.4.18

# Scheduling
apscheduler>=3.10.4
